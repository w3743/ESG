# 第4批次 ESG 报告解析归档

本批次采集并解析 50 份 2024 年中文 ESG、可持续发展或环境社会治理报告，每份报告对应不同公司和证券代码。当前校验状态：**通过**。

## 文件

- `第4批次_解析结果.7z`：50 个独立报告目录及完整 MinerU 输出，包括 `_origin.pdf`、Markdown、JSON 和图片。
- `第4批次_清单.tsv`：报告元数据、官方来源、下载信息、页数、PDF SHA-256、规范化文本指纹和解析状态。
- `第4批次_校验报告.json`：逐份验收结果、唯一性检查和归档校验。

## 采集与准入

来源优先使用公司官网，其次使用 CNINFO、上交所、深交所或港交所等正式披露渠道；清单保留每份官方 URL 和下载时间。候选已排除第 3 批次及现有项目中的公司报告，并按公司/证券代码、PDF SHA-256、规范化正文指纹去重。

## MinerU

标准命令：`mineru -p <input> -o <output> -b pipeline -m txt -l ch -t false`。

仅对 TXT Markdown 为空或核心输出不完整的报告使用 OCR 回退：`mineru -p <pdf> -o <output> -b pipeline -m ocr -l ch -t false`。

本次解析方式统计：{"pipeline-ocr": 2, "pipeline-txt": 48}。MinerU 运行显式使用本机 CUDA GPU；未启动模型训练、Arena 或分类评估。

## 注意事项

报告仍受原披露来源的版权和再分发条款约束；公开可访问不等于取得独立镜像授权。研究使用请回溯清单中的官方来源 URL。

生成时间：`2026-08-31T16:03:45+08:00`
