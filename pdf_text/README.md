# ESG parsed text

This directory contains only direct MinerU parsed report text. Original PDFs,
tables, metadata JSON, source manifests, images, layout artifacts, archives,
training data, logs, and temporary files are not published here.

Each report uses `pdf_text/batch-<NN>/<ticker>/` and contains only `report.md`.
A batch is published only after its 50-report text validation passes.
